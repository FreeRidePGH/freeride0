<%= form_for(@bike_model) do |f| %>
  <% if @bike_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bike_model.errors.count, "error") %> prohibited this bike_model from being saved:</h2>

      <ul>
      <% @bike_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_id %><br />
    <%= collection_select(:bike_model, :brand_id, BikeBrand.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
