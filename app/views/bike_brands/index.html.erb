<h1>Bike Brands</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bike_brands.each do |bike_brand| %>
  <tr>
    <td><%= bike_brand.name %></td>
    <td><%= link_to 'Edit', edit_bike_brand_path(bike_brand) %></td>
    <td><%= link_to 'Destroy', bike_brand, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bike brand', new_bike_brand_path %>
